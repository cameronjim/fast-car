# docs

Human-facing project documentation, distinct from `claude-docs/` which governs how Claude
Code works in this repo. `conventions.md` (added in roadmap task 0.8) will seed from
`claude-docs/10-conventions.md`; `notes/` holds per-phase running notes written as things
happen, since the project writeup is continuous and Phase 7 is only assembly. See
`claude-docs/02-repo-layout.md` and `claude-docs/10-conventions.md`.
